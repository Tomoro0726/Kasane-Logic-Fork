use bincode::Encode;

use crate::{encode_id_set::EncodeIDSet, point::Coordinate, space_time_id::SpaceTimeID};

fn route(z: u8, start: Coordinate, end: Coordinate, ban_list: EncodeIDSet) -> EncodeIDSet {
    todo!()
}
